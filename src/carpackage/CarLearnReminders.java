package carpackage;

public class CarLearnReminders {
    void differentThings () {
        // ternary operator
        // A ternary operator is checking 2 arguments and then providing the result depending on the ckeck
        int a = 13;
        int b = 10;
        // Here is the ternary operator
        int max = a > b ? a : b;
        System.out.println("max = " + max);
    }
}
